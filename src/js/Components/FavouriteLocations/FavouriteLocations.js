import Component from '../../Framework/Component';

export default class FavouriteLocations extends Component {
    constructor(host, props) {
        super(host, props);
    }

    render() {
        return [
            'I am a FavouriteLocations component',
        ]
    }
}
